## Mobile Application Quickstart Steps (ios simulator)
Navigate directly inside the target directory:
   ```bash
   cd app
   npm install
   npx expo start --clear
   i
   ```
To execute unit tests using vitest, run:
    ```bash
    npm test
    ```
